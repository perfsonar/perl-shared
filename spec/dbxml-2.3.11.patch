diff -ur dbxml-2.3.11/buildall.sh rpm/SOURCES/dbxml-2.3.11/buildall.sh
--- dbxml-2.3.11/buildall.sh	2007-02-05 11:34:33.000000000 -0500
+++ rpm/SOURCES/dbxml-2.3.11/buildall.sh	2009-03-13 10:14:30.000000000 -0400
@@ -474,12 +474,12 @@
     echo "for configuration and build instructions"
     cd $DBXML_DIR/src/perl
     if [ $DO_CONFIGURE = "yes" ]; then
-	$PERL Makefile.PL
+	$PERL Makefile.PL PREFIX=$DBXML_INSTALL_DIR
     fi
     echo Building Perl
     $MAKE || exit $?
-    echo Testing Perl
-    $MAKE test || exit $?
+#    echo Testing Perl
+#    $MAKE test || exit $?
     if [ $DO_INSTALL = "yes" ]; then
 	echo Installing Perl
 	$MAKE install || exit $?
